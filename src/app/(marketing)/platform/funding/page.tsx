/**
 * /platform/funding — placeholder per WeConnect v1 scope.
 * See CLAUDE.md: "Funding tab: Placeholder page only"
 */
import ComingSoon from '@/components/ComingSoon'

export default function FundingPage() {
  return <ComingSoon section="Funding" />
}
